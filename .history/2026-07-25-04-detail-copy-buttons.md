# 04 — 详情字段逐行复制按钮 — 2026-07-25

## 概述
`PasswordDetailDialog` 中为每个详情字段（用户名/密码/网址/备注）右侧添加独立复制按钮，靠右对齐。底部移除冗余的"复制密码"按钮，保留编辑和删除。

## 改动

| 文件 | 改动 |
|------|------|
| `PasswordDetailDialog.kt` | `DetailField` 改为 Row 布局：左侧标签+值(weight)，右侧 IconButton(复制)；底部精简为编辑+删除 |

## 验证状态

- [x] 编译通过
