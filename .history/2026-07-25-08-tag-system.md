# 08 — 标签系统集成 — 2026-07-25

## 概述
完善标签系统：数据层添加标签 CRUD 和关联查询，ViewModel 管理标签状态，Create/Detail/Home/Search 页面全线集成标签功能。

## 改动

### 数据层

| 文件 | 改动 |
|------|------|
| `PwdDao.kt` | TagDao 新增 `getByName`、`deleteJoinsForPassword`、`insert` 返回 `Long` |
| `PwdRepository.kt` | 新增 `tagDao` 参数；`insertTag`/`setTagsForPassword`/`addTagToPassword` 等方法 |
| `PwdViewModel.kt` | 新增 `allTags`、`tagMap` StateFlow；`refreshTagMap`、标签 CRUD 方法 |
| `PwdViewModelFactory.kt` | 注入 `TagDao` |

### UI 层

| 文件 | 改动 |
|------|------|
| `CreatePasswordScreen.kt` | 标签区域：InputChip 显示 + TextField 输入 + Add 按钮；保存时调用 `setTagsForPassword` |
| `PasswordDetailDialog.kt` | 新增 `tags` 参数；SuggestionChip 行显示标签 |
| `HomeScreen.kt` | `detailItem` 弹窗传入 `tagMap` 标签 |
| `SearchScreen.kt` | 搜索范围加入标签匹配；弹窗传入标签 |

## 验证状态

- [x] 编译通过
